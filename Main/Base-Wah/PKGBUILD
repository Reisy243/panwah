# Maintainer: Panwah Team
# Contributor: Isaac (Reisy RedPanda) <opensrcrdp@gmail.com>
# Repo: Main
pkgname="Base-Wah"
pkgver=Alpha_1.1
pkgrel=1
pkgdesc="WArch base package"
arch=("any")
url="https://reisy243.github.io/panwah"
license=("unknown")
groups=("Panwah")
depends=(
	# Essentials packages
	"FileSystem" "gcc-libs" "glibc" "zsh"
	# POSIX tools
	"coreutils" "file" "findutils" "gawk" "grep" "procps-ng" "sed" "tar"
	# standard linux toolset
	"gettext" "pciutils" "psmisc" "shadow" "util-linux" "bzip2" "gzip" "xz"
	# Distro things
	"licenses" "pacman" "archlinux-keyring" "systemd" "systemd-sysvcompat"
	# Network things
	"iputils" "iproute2"
)
optdepends=(
	"Linux-Wah: Default Panwah Linux kernel"
)
provides=("BaseOS")
